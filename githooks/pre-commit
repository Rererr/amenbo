#!/bin/sh
# 秘密情報（gitleaks）を検査する
set -e

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "pre-commit エラー: gitleaks が未インストール (brew install gitleaks)" >&2
  exit 1
fi

gitleaks git --pre-commit --staged --redact
